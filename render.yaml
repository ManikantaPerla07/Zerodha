services:
  - type: web
    name: zerodha
    env: node
    plan: free
    buildCommand: rm -rf node_modules backend/node_modules dashboard/node_modules && npm install --prefix backend && npm install --prefix dashboard && npm run build --prefix dashboard
    startCommand: node backend/index.js
    autoDeploy: true